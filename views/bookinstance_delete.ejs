<!DOCTYPE html>
<html>
    <%-  include('./partials/head.ejs')%>
  <body>
    <!-- Sidebar -->
    <%-  include('./partials/sidebar.ejs')%>
    <div class="content-page">
        <h1><%= title %>: <%= bookinstance.book.title %></h1>
        <p><b>Imprint:</b> <%= bookinstance.imprint %></p>

        <p>
            <b>Status:</b>
            <% if (bookinstance.status === 'Available') { %>
                <span class="text-success">
                    <%= bookinstance.status %>
                </span>
            <% } else if(bookinstance.status === 'Maintenance') {%>
                <span class="text-danger">
                    <%= bookinstance.status %>
                </span>
            <% } else {%>
                <span class="text-warning">
                    <%= bookinstance.status %>
                </span>
            <% } %>
        </p>

        <% if (bookinstance.status != 'Available') { %>
             <p>
                <b>Due back:</b>
                <%= bookinstance.due_back_formatted %>
             </p>
        <% } %>

            <p>Do you really  want to delete this Instance?</p>
            <form method="POST" action="">
                <div class="form-group">
                  <input class="form-control" id="bookinstanceid" type="hidden" name="bookinstanceid" required="true" value="<%= bookinstance._id %>"/>
                </div>
                <button class="btn btn-primary" type="submit">Delete</button>
              </form>
    </div>
  </body>
</html>